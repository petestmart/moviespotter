<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application light theme. -->
    <!-- themes.xml files only used for status bar -->
    <!-- See [Theme.kt] for theme configurations -->
    <style name="Theme.MovieSpotter" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:statusBarColor"
            tools:targetAPI="1">@color/primaryLightColor</item>
    </style>
</resources>