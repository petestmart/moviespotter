<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application dark theme. -->
    <!-- themes.xml files only used for status bar -->
    <!-- See [Theme.kt] for theme configurations -->
    <style name="Theme.MovieSpotter" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:statusBarColor"
            tools:targetAPI="1">@color/primaryDarkColor</item>
    </style>
</resources>